{{- $name := printf "%s-credentials" (include "loans-database.fullname" .) -}}
{{- $existing := lookup "v1" "Secret" (include "loans-database.namespace" .) $name -}}
{{- $password := .Values.auth.password -}}
{{- if and (not $password) $existing -}}
{{- $password = (index $existing.data "postgres-password" | b64dec) -}}
{{- else if not $password -}}
{{- $password = randAlphaNum 32 -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $name }}
  namespace: {{ include "loans-database.namespace" . }}
  labels: {{- include "loans-database.labels" . | nindent 4 }}
type: Opaque
stringData:
  postgres-user: {{ .Values.auth.username | quote }}
  postgres-password: {{ $password | quote }}
  database-url: {{ printf "postgresql://%s:%s@%s:%v/%s?sslmode=disable" .Values.auth.username $password (include "loans-database.fullname" .) .Values.service.port .Values.auth.database | quote }}
